============
Installation
============

At the command line::

    $ easy_install nectar-metrics

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv nectar-metrics
    $ pip install nectar-metrics